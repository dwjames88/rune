# Alternate app icons

Drop Icon Composer `.icon` bundles here and they become selectable app-icon
options in Rune's Settings → Appearance → App Icon (in every build).

The Dev icon lives in `Assets/Dev Icon` instead — it's the dev build's own
icon and is only offered as an option in dev builds (see scripts/dev-run.sh).
